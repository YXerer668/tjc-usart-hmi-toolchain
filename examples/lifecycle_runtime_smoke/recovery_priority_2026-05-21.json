{
  "schema_version": 1,
  "date": "2026-05-21",
  "target": "TJC8048X543_011C",
  "status": "prepared",
  "top_priority_after_panel_recovery": {
    "id": "recover_then_seed_side_orchestrator",
    "reason": "the page1 file-browser compiled-data envelope is largely exhausted offline, and the latest real run proved the panel is still transport-silent after a local official-recovery attempt. the next highest-value move is therefore one orchestrated entrypoint that re-checks serial health, retries the official recovery lane when needed, and only then runs the seed-side runtime limitation discriminator.",
    "command": "python tools/recover_then_run_seed_side_runtime_limit.py --out-dir reverse_usarthmi/recover_then_seed_side_run_20260521 --capture"
  },
  "secondary_priority_after_panel_recovery": {
    "id": "seed_side_runtime_limitation_falsification",
    "reason": "use the direct runner only when the panel is already responsive or when you intentionally want to bypass the integrated recovery step.",
    "command": "python tools/run_seed_side_multipt_runtime_limit_smokes.py --out-dir reverse_usarthmi/seed_side_runtime_limit_run_20260521 --with-textselect-control"
  },
  "notes": [
    "The latest real orchestrated run still ended in connect/sendme silence after an official GUI recovery attempt, so the next step now requires physical or otherwise external recovery before more local live automation can help.",
    "Do not continue large batches of page-local byte patching before the orchestrated recovery-plus-seed-side path has been exercised on recovered hardware.",
    "Within the seed-side runner, text-select is the strongest control sample, file-stream is second, and file-browser is the target falsification sample."
  ]
}
